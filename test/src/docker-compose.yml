version: '3'

services:
  nginx:
    build: requirements/nginx
    container_name: nginx
    image: nginx
    #networks:
    #  - inception
        #volumes:
        #  - wordpress_data:/var/www/html/wordpress
        #restart: always

    #networks:
    #inception:
    #driver: bridge

    #volumes:
    #mariadb_data: {}
    #driver: local
    #driver_opts:
    # type: none
    # device: ~/inception_vol/data/mariadb
    # o: bind

    # wordpress_data: {}
    # driver: local
    # driver_opts:
    #   type: none
    #   device: ~/inception_vol/data/wordpress
    #   o: bind
